<Window x:Class="SpeekerTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpeekerTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="출력 장치:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="DeviceCombo" Width="420" />
            <Button Content="목록 새로고침" Margin="8,0,0,0" Click="BtnRefresh_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="방송 문구:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="MessageBox" Width="420" Text="안녕하세요"/>
            <Button Content="송출하기" Margin="8,0,0,0" Click="BtnSpeak_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="로그:" Margin="0,0,0,4"/>
        <TextBox Grid.Row="3" x:Name="LogBox" IsReadOnly="True" AcceptsReturn="True"
             VerticalScrollBarVisibility="Auto"/>
    </Grid>


</Window>
